# The benchmark fixture is evidence, not the product. Marking it vendored keeps the
# language bar describing the plugin instead of the Python it happens to measure.
bench/project/** linguist-vendored
